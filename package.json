{
  "name": "web-learing",
  "version": "1.0.0",
  "description": "summary",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cz": "npx git cz",
    "lint-staged": "lint-staged",
    "commitlint": "commitlint --config commitlint.config.js -e -V",
    "release": "standard-version",
    "lint": "npx eslint --ext .js",
    "lint-fix": "npx eslint --fix --ext .js src"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xupeng-faker/web-learning.git"
  },
  "lint-staged": {
    "src/**/*.{js,vue,md,json}": [
      "eslint --fix",
      "prettier --write",
      "git add"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "commit-msg": "commitlint -e $GIT_PARAMS"
    }
  },
  "keywords": [
    "note"
  ],
  "author": "xupeng-faker",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/xupeng-faker/web-learning/issues"
  },
  "homepage": "https://github.com/xupeng-faker/web-learning#readme",
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@commitlint/config-conventional": "^12.1.4",
    "commitizen": "^4.2.4",
    "cz-conventional-changelog": "^3.3.0",
    "eslint": "^7.29.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^3.4.0",
    "eslint-plugin-vue": "^7.11.1",
    "lint-staged": "^11.0.0",
    "prettier": "^2.3.1"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "dependencies": {
    "husky": "^6.0.0",
    "standard-version": "^9.3.0"
  }
}
